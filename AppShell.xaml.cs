using MauiAppMVVM.Views;

namespace MauiAppMVVM
{
    public partial class AppShell : Shell
    {
        public AppShell()
        {
            InitializeComponent();
            /* Rutas De Acceso
            Routing.RegisterRoute(nameof(PageListProductos), typeof(PageListProductos));
            Routing.RegisterRoute(nameof(VerProductPage), typeof(VerProductPage)); */
        }
    }
}
